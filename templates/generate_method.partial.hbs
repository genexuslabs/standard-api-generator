
/**
{{#if description}}
 * {{description}}
{{/if}}
{{#each parameters}}
 * @param {{sanitizeName name}}
{{/each}}
{{#if returns}}
 * @return {{mapType returns}}
{{/if}}
 */
{{#if static}}static {{/if}}{{sanitizeName name}}(...args: any[]){{#if returns}}: any{{/if}} {
  notImplemented("{{sanitizeName name}}");{{#if returns}}
  return null;{{/if}}
}
